# ==============================================================================
# AEGIS Voice-Sentinel — CORE Python dependencies (backend.main)
#
# Optional feature extras (STT, Gemini AI, PDF, Postgres, dev tools) are
# declared in pyproject.toml — install with:
#     pip install -e ".[stt,ai,pdf,dev]"
# ==============================================================================

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
torch>=2.2.0
torchaudio>=2.2.0
soundfile>=0.12.1
scipy>=1.12.0
numpy>=1.26.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.6.0
SpeechRecognition>=3.10.0
httpx>=0.27.0
